node.exe : npm warn Unknown env config "devdir". This will stop working in the next major version of npm. See `npm 
help npmrc` for supported config options.
At line:1 char:1
+ & "C:\Program Files\nodejs/node.exe" "C:\Program Files\nodejs/node_mo ...
+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (npm warn Unknow...config options.:String) [], RemoteException
    + FullyQualifiedErrorId : NativeCommandError
 
React Doctor v0.5.8

  

  ΓÜá Bugs: Array index used as a key
    Your users can see & submit the wrong data when this list
    reorders or filters, so use a stable id like
    `key={item.id}`, not the array index "i".
    ΓåÆ Use a stable id from the item, like `key={item.id}` or
    `key={item.slug}`. Index keys break when the list reorders
    or filters.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-array-index-as-key

    src/components/survey/survey-detail-sections.tsx:117

  ΓÜá Bugs: Missing effect dependencies ├ù3
    `useCallback` re-runs whenever `isSignedIn, orgId, orgRole,
    authEpoch` changes even though it never uses it.
    ΓåÆ Don't blindly add missing dependencies. Read the hook
    callback first.
    
    Bad:
    useEffect(() => {
      setCount(count + 1);
    }, [count]);
    
    Better:
    useEffect(() => {
      setCount((currentCount) => currentCount + 1);
    }, []);
    
    If the missing value is recreated every render, move it
    inside the hook or stabilize it before adding it to deps.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/exhaustive-deps

    src/hooks/use-auth-for-convex.ts:321

    src/hooks/use-sync-convex-user.ts:64

    src/hooks/useDebouncedCloudSave.ts:42

  ΓÜá Bugs: Time or random value in JSX
    This can cause a hydration mismatch because Date.now()
    reached from JSX gives a different value on the server than
    in the browser. Move it into useEffect+useState to run only
    in the browser, or add suppressHydrationWarning to the
    parent if it's on purpose.
    ΓåÆ Move time or random values into useEffect+useState so
    they only run in the browser, or add
    suppressHydrationWarning to the parent if it's intentional
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/rendering-hydration-mismatch-time

    src/components/admin/assign-user-sections.tsx:146

  ΓÜá Performance: await inside a loop ├ù3
    This makes the for-loop slow because each await runs one
    after another, so collect the independent calls & run them
    together with `await Promise.all(items.map(...))`
    ΓåÆ Collect the items, then use `await
    Promise.all(items.map(...))` so independent work runs at
    the same time
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/async-await-in-loop

    src/hooks/use-auth-for-convex.ts:290

    src/hooks/useWizardPhotoCapture.ts:151

    src/utils/captureGps.ts:192

  ΓÜá Bugs: Prop derived into useState
    Your users see a stale value when prop "previewUri" changes
    because useState copies it once.
    ΓåÆ Compute the value inline so prop changes do not leave
    `useState` holding a stale copy.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-derived-useState

    src/components/ui/primitives.tsx:1103

  ΓÜá Bugs: Non-virtualized mapped list in ScrollView ├ù3
    Your users get slow scrolling when <ScrollView> with
    items.map(...) builds every row at once.
    ΓåÆ `<ScrollView>{items.map(...)}</ScrollView>` builds every
    row at once, which slows scrolling. Use FlashList,
    LegendList, or FlatList instead.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/rn-no-scrollview-mapped-list

    src/components/admin/filter-chips.tsx:27

    src/components/config-gate.tsx:24

    src/components/ui/primitives.tsx:790

  ΓÜá Bugs: Derived value copied into state ├ù2
    Storing "sqftText" in state when you can derive it from
    other values costs an extra render.
    ΓåÆ Work out the value while rendering (or with useMemo if
    it's expensive) instead of copying it into useState through
    a useEffect. See
    https://react.dev/learn/you-might-not-need-an-effect#updating-state-based-on-props-or-state
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-derived-state

    src/components/ui/primitives.tsx:969-970

  ΓÜá Bugs: Multiple setState calls in one effect
    8 setState calls in one useEffect redraw your screen each
    time they run together.
    ΓåÆ Combine related updates in `useReducer` so one effect
    does not redraw the screen once per `setState` call.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-cascading-set-state

    src/hooks/use-clerk-convex-auth.ts:61

  ΓÜá Performance: State only used in handlers
    Each update to "lastPreviewUri" redraws your component for
    nothing because this useState is set but never shown on
    screen.
    ΓåÆ Use useRef instead of useState when the value is only set
    and never shown on screen. `ref.current = ...` updates it
    without redrawing the component.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/rerender-state-only-in-handlers

    src/components/ui/primitives.tsx:1103

  ΓÜá Bugs: Inline renderItem on list ├ù3
    Your users see extra row work when renderItem on <FlatList>
    is rebuilt every time the screen redraws.
    ΓåÆ Move renderItem to a named function or wrap it in
    useCallback so it is not rebuilt every time the screen
    redraws.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/rn-no-inline-flatlist-renderitem

    src/app/(admin)/users.tsx:123

    src/app/(app)/qc/[id].tsx:108

    src/components/ui/primitives.tsx:346

  ΓÜá Bugs: Event logic handled in an effect ├ù2
    Faking an event handler with a prop plus a useEffect costs
    an extra render & runs late.
    ΓåÆ Run the side effect in the event handler that triggers
    it, instead of watching its state from a useEffect. See
    https://react.dev/learn/you-might-not-need-an-effect#sharing-logic-between-event-handlers
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-event-handler

    src/components/layout-guard.tsx:22

    src/hooks/use-clerk-convex-auth.ts:98

  ΓÜá Bugs: State updates chained through effects ├ù8
    Chaining state updates triggers an extra render each step.
    ΓåÆ Set all the related state together in the event handler
    that starts it, instead of having one useEffect react to a
    state change and set more state. See
    https://react.dev/learn/you-might-not-need-an-effect#chains-of-computations
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/no-chain-state-updates

    src/hooks/use-clerk-convex-auth.ts:69-70

    src/hooks/use-clerk-convex-auth.ts:76-77

    src/hooks/use-clerk-convex-auth.ts:82

    src/hooks/use-clerk-convex-auth.ts:94

    src/hooks/use-clerk-convex-auth.ts:103

    src/hooks/use-clerk-convex-auth.ts:107

  ΓÜá Performance: Empty default prop breaks memo
    This keeps redrawing children that compare props because
    default prop value [] makes a brand new array every render,
    so move it to a constant at the top of the file
    ΓåÆ Move it to the top of the file: `const EMPTY_ITEMS:
    Item[] = []`, then use that as the default value
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/rerender-memo-with-default-value

    src/components/ui/primitives.tsx:258

  ΓÜá Bugs: JS-thread animation instead of Reanimated ├ù2
    Your users see stutter when Animated from react-native runs
    on the JS thread.
    ΓåÆ Use `import Animated from 'react-native-reanimated'` so
    animations run on the UI thread instead of the JS thread,
    which keeps them smooth.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/rn-prefer-reanimated

    src/components/brand-logo.tsx:3

    src/components/ui/primitives.tsx:19

  ΓÜá Performance: Ref initializer runs on every render ├ù3
    useRef(new Value()) rebuilds this value on every render &
    throws it away.
    ΓåÆ Initialize the ref lazily so expensive values are not
    rebuilt and discarded on every render.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/rerender-lazy-ref-init

    src/components/brand-logo.tsx:27

    src/components/ui/primitives.tsx:521

    src/components/ui/primitives.tsx:599

  ΓÜá Bugs: Legacy platform-specific shadow styles
    Shadow styles "shadowColor", "shadowOffset",
    "shadowOpacity", "shadowRadius", "elevation" only work on
    one platform, so your users on the other see no shadow.
    ΓåÆ Use `boxShadow` for shadows that work on both platforms
    on the new architecture, instead of platform-specific
    shadow properties.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/rn-no-legacy-shadow-styles

    src/components/brand-logo.tsx:59

  ΓÜá Maintainability: Static value rebuilt every render ├ù5
    `baseHeights` inside `AppButton` uses no local state but is
    rebuilt every render, so it looks new each time & breaks
    memoized children. Move it to the top of the file, outside
    the component.
    ΓåÆ Move the value above the component, at the top of the
    file. It doesn't use local state, so rebuilding it each
    update is wasted and makes it look new every time.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/prefer-module-scope-static-value

    src/components/ui/primitives.tsx:95

    src/components/ui/primitives.tsx:100

    src/components/ui/primitives.tsx:106

    src/components/ui/primitives.tsx:112

    src/components/ui/primitives.tsx:656

  ΓÜá Performance: await before an early-return guard
    This await blocks the function before an early-return that
    doesn't use the awaited value, so the skip path waits for
    nothing. Move the await below the guard so it only runs
    when you need the data
    ΓåÆ Move the `await` below the early-return guard so the skip
    path stays fast and avoids unnecessary async work.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/async-defer-await

    src/hooks/useWizardPhotoCapture.ts:205

  ΓÜá Maintainability: deslop/unused-export
    Unused export: `useTabScreenPadding` is exported but no
    module imports it, so it expands the public surface and can
    mislead callers about supported API.
    ΓåÆ Drop the `export` keyword (or remove the declaration) if
    no other module uses this symbol.

    src/utils/ui-layout.tsx:6

  ΓÜá Bugs: Inline handler in list renderItem ├ù3
    This onPress is rebuilt for every row, so your memo() rows
    still redraw.
    ΓåÆ Move the handler out with useCallback at list scope and
    pass the row id as a prop. Then memo() rows skip redrawing
    when their data has not changed.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/rn-list-callback-per-row

    src/app/(admin)/users.tsx:125

    src/app/(app)/qc/[id].tsx:119

    src/components/ui/primitives.tsx:350

  ΓÜá Performance: JSX element passed as a prop
    This child redraws every render because the prop gets brand
    new JSX each time.
    ΓåÆ Move the JSX outside the component or wrap it in
    `useMemo` so memoized children do not redraw every render.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/jsx-no-jsx-as-prop

    src/app/(admin)/users.tsx:112

  ΓÜá Performance: .map().filter(Boolean) loops twice
    This loops over your list twice because
    .map().filter(Boolean) makes two passes, so use .flatMap()
    to change & drop items in one pass
    ΓåÆ Use `.flatMap(item => condition ? [value] : [])` to
    change and drop items in one pass, instead of building a
    throwaway array in between
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/js-flatmap-filter

    src/components/ui/primitives.tsx:504

  ΓÜá Bugs: Inline object in list renderItem ├ù2
    This object is rebuilt for every row, so your memo() rows
    still redraw.
    ΓåÆ Move style and object props out of renderItem
    (StyleSheet.create, useMemo at list scope, or pass
    primitives) so memo() rows stop redrawing when their data
    has not changed.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/rn-no-inline-object-in-list-item

    src/components/ui/primitives.tsx:356-358

  ΓÜá Performance: Chained array iterations ├ù4
    This loops over your list twice because .map().filter()
    makes two passes, so do it in one pass with .reduce() or a
    for...of loop
    ΓåÆ Combine `.map().filter()` style chains into one pass with
    `.reduce()` or a `for...of` loop, so you only loop over the
    list once
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/js-combine-iterations

    src/hooks/useSaveSurveyDraft.ts:37

    src/hooks/useSaveSurveyDraft.ts:59

    src/hooks/useWizardDraft.ts:472

    src/hooks/useWizardDraft.ts:567

  ΓÜá Bugs: Platform-specific shadow keys over boxShadow
    Your users on the other platform see no shadow when you use
    shadowColor.
    ΓåÆ These shadow keys only work on one platform. On RN v7+,
    use the CSS `boxShadow` string instead, like `boxShadow: "0
    2px 8px rgba(0,0,0,0.1)"`, which works on both.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/rn-style-prefer-boxshadow

    src/components/brand-logo.tsx:60

  ΓÜá Maintainability: Non-component export in component file ├ù5
    This file exports non-components, so Fast Refresh can't
    safely preserve component state.
    ΓåÆ Move non-component exports out of component files so Fast
    Refresh can preserve component state instead of
    full-reloading.
    Curl with no cache & follow the canonical fix and false positive check recipe before fixing: https://react.doctor/docs/rules/react-doctor/only-export-components

    src/utils/ui-layout.tsx:22

    src/utils/ui-layout.tsx:32

    src/utils/ui-layout.tsx:42

    src/utils/ui-layout.tsx:52

    src/utils/ui-layout.tsx:62


  ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ

  All 60 issues

  Bugs > 34 warnings
  Performance > 15 warnings
  Maintainability > 11 warnings

Agent guidance
  - Treat React Doctor diagnostics as starting hypotheses. Read the relevant code before confirming or suppressing each finding.
  - For each group, decide true positive, false positive, or needs-human-review, then assign high/medium/low confidence.
  - Do not suppress a finding without evidence from the file in question. Confidence requires code context.
  - Understand the root cause before editing. Fix the underlying code instead of changing react-doctor config or suppressing rules unless explicitly asked.
  - Investigate deeply where relevant: race conditions, security-sensitive flows, state propagation, multi-file refactors, and downstream dependency chains.
  - Ignore pure style preferences, theoretical issues without real impact, missing features, and unrelated pre-existing code.
  - Start with high-confidence fixes that preserve behavior. Leave low-confidence or product-dependent changes as notes.
  - Run `npx react-doctor@latest --verbose --scope changed` before and after changes, plus relevant tests after each focused batch.
  - When available, spawn subagents or isolated worktrees for independent rule families, then review and merge only the best safe fixes.
  - Split unrelated, broad, or behavior-changing work into separate PRs/branches instead of one large cleanup.
  - When one rule spans dozens of files (a migration-scale change), fix a representative sample first, confirm the recipe holds, and get the code owner's sign-off before changing the rest. Don't mass-fix a broad pattern in one unreviewed pass.
  - For confirmed issues that cannot be fixed now, create GitHub issues with the rule, file/line, confidence, impact, and proposed fix.
  - If a fix needs an API, UX, or architecture decision, stop and ask before editing.

  ΓöîΓöÇΓöÇΓöÇΓöÇΓöÇΓöÉ  63 / 100 Needs work
  Γöé ΓÇó ΓÇó Γöé  ΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûêΓûæΓûæΓûæΓûæΓûæΓûæΓûæΓûæΓûæΓûæΓûæΓûæΓûæΓûæΓûæΓûæΓûæΓûæ
  Γöé  ΓöÇ  Γöé  React Doctor (https://react.doctor)
  ΓööΓöÇΓöÇΓöÇΓöÇΓöÇΓöÿ

  Full diagnostics written to C:\Users\sikar\AppData\Local\Temp\react-doctor-510d5f7c-8338-4463-ab9b-8a51250ff541

  ΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇΓöÇ

  Share: https://react.doctor/share?p=survey-app&s=63&w=60&f=18
  Tell others how you did on socials

  Docs: https://react.doctor/docs
  Learn more about fixing issues, setting up CI/CD, and
  configuring rules with a config file

  GitHub: https://github.com/millionco/react-doctor
  Report issues and star the repository!

